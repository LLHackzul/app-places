import 'package:flutter/material.dart';

class OutstandingPlacesScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text('Los Destacados')
      ),
    );
  }
}